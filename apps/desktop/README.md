# `apps/desktop`

Tauri + React desktop shell. **Not implemented in Phase 0 STEP 1–2.**

Do not add UI, windowing, or native bindings until Product Owner authorizes
STEP 4 (Basic React UI Shell) in `ROADMAP.md`.
