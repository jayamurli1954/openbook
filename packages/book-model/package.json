{
  "name": "@openbook/book-model",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Canonical OpenBook Book Model. EPUB is not this package.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/book-model.test.js"
  }
}
