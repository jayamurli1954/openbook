# Tests

- `fixtures/` — Book Model JSON (authoring documents, not EPUB packages).
- `golden/` — reserved for later publishing golden files (STEP 6+).
- `integration/` — reserved for later cross-package tests.

Do not commit private manuscripts.
